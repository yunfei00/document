# Python Project Template（企业级轻量模板）

包含：
- argparse 子命令
- logging 统一配置（-v/-vv）
- workdir（Path）
- init-workdir 示例命令

运行：
```bash
python -m project.cli.main init-workdir -v
python -m project.cli.main train --epochs 50 -vv
```
