# 版本修改说明

## V1.0.0
	实现基本功能

## V1.0.1
	实现登录功能

## V1.0.2 
	容器里面添加vim, spotbugs

## V1.0.3 
	1. 实现默认仓库名文件上传
	2. 添加docker客户端到镜像中

## V1.0.4 
	支持env中配置仓库名称，实现文件上传

## V1.0.5 
	1. 根据仓库IP地址，导入现有仓库信息
	2. 检测工具未配置---生成虚拟报告
	3. spotbugs 生成jar文件检测报告
	4. 岗位管理不能删除
	5. 所有用户显示的都是全部菜单。
		修改建议：页面只有超管权限的账号才能显示全部菜单，其他账户在申请通过时应该是白屏不应该是全部菜单显示。提交的申请只有拥有用户审批权限的人才能看到和审批。别的账户需要显示什么菜单得要超管在创建账号时勾选。
	6. 新建项目时，部门、最终用户、待审批这三条不需要
	7. 挂载外部的db,reports,media,更新软件后，数据存在
	8. 镜像上传时，关联到仓库，在镜像详情页，下载报告
	9. 业务镜像上传与报告下载功能
	10. 申请用户的角色没有和数据库关联起来
   

## V1.0.6 
	1. 基础镜像上传，支持大文件传输
	2. 业务镜像上传，支持大文件传输
	3. 镜像上传后，删除docker加载的文件


## V1.0.7
	1. 查看仓库内的镜像文件信息
   
## V1.0.8
	1. 仓库内显示所有镜像，包括版本信息，并提供下载接口
	2. jar包检测，支持大文件检测

## 1.0.9
    1. 项目权限细分 主要是创建和审批权限
    2. 创建项目时，可以添加多个基础镜像信息
    3. 加签
   
## 1.1.0
    1. 查看镜像，权限控制，从用户角度出发，用户所在项目，查看相关镜像信息，仓库镜像过滤

## 1.1.1
	1. 创建项目时，隐藏项目编码，后台自动生成
	2. 下载项目时，使用项目名称+镜像名称+版本 下载
	3. 验签
	4. 添加审批通知--项目审批，用户审批
	5. 用户先把镜像传上去，然后操作那栏的四个按钮里没有下载那个按钮，等到后台检测完，如果管理员点了通过，用户账号才有下载按钮，不通过就没有下载按钮 
	6. 审批通知里面添加项目审批详情
	7. 版本信息折叠显示
	8. 项目名称 增加唯一性约束
	9.  上传业务镜像时，项目不显示编码，只需要项目名称即可

## 1.1.2
	1. 主页中，系统基础镜像不显示 
	2. 仓库镜像，只留镜像和操作，点开后要有路径
   
## 1.1.3
	1. 用户上传业务镜像后，需要检测结果出来后，admin检测结果审核通过，才可以用户下载
   
## 1.1.4
	1. 创建项目的时候把basic里面的镜像都展示出来

## 1.1.5
	1. 修复仓库同步后，业务镜像被误同步到基础镜像的bug

## 1.1.6
	1. 运维人员这个角色具有同admin一样的权限，但是不需要显示全量菜单，个人主页不要显示了
	2. 仓库镜像显示 添加上传时间
	3. 检测状态和审批状态改成“已/未完成检测”和“已/未通过”
	4. 基础镜像可以删除，没有权限时进行提示
	5. 业务镜像管理部分，不需要下载业务镜像，改成同步，并后台同步到镜像仓库

## 1.1.7 
	1. 业务镜像管理审批状态已通过才能点击同步，否则无法点击
	2. 上传业务镜像这里需要写死在business-prod，不要下拉菜单 
	3. 运维人员这个角色添加admin的审批功能，菜单只保留镜像管理和仓库管理
	4. 运维人员的账号添加同步索引按钮

## 1.1.8 
	1. 仓库镜像不需要显示版本数量
	2. 点击同步仓库索引后，上传时间使用仓库中获取的时间文件
	3. 仓库里执行过删除操作，点击同步仓库索引后，删除不存在的镜像
	4. 同步功能，把业务镜像同步到业务镜像仓库

## 1.1.9
	1. 大文件上传优化，支持分片上传