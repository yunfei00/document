# 下载命名规则（交付一致性）

## 规则
- 业务镜像：项目名称 + 镜像名称 + 版本
- 基础镜像：项目名称使用“基础镜像”或固定名（按你约定）

## 例子
- <project>_<image>_<version>.tar
- base_<image>_<version>.tar（如采用 base）

## 注意
- 文件名字符集：建议只用 [a-zA-Z0-9._-]
- 长度限制与截断策略
